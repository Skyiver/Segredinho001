version: '3.8'

services:
  web:
    build: .
    ports:
      - "5002:5002"
    volumes:
      - .:/code
      - ./db:/code/db  
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - SQLALCHEMY_DATABASE_URI=sqlite:////code/db/app.db